using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterNecromancer : Character
{
    // Start is called before the first frame update
    void Start()
    {
        //cost = 
        hitPoints = 25;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
